# TaskFlow API - Backend Documentation

This is the secure REST API backend for the TaskFlow application. It handles user authentication, secure sessions via JWT, and full CRUD operations for the task board.

## 🚀 Getting Started

### Prerequisites
* Node.js installed
* MySQL running locally

### Installation
1. Clone the repository.
2. Install the dependencies:
   ```bash
   npm install
   